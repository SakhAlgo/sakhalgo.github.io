1.
добавь в курс html и css новый topic "Правила и селекторы CSS"  и придумай три простых задания на тему селекторов. Напирмер найди ошибку в селекторе или дописать селектор.
Напиши валитадоры.


2.
в курсе js, топик js conditionals добавь задание:  (название: Тебе нравится JS?
Само задание:
Необходимо задать пользователю вопрос, нравится ли ему JavaScript?

Если пользователь ответит положительно, необходимо вывести сообщение "Конечно, отличный язык."

Если пользователь ответил отрицательно, необходимо вывести сообщение "Не нравится? Вы кнопкой ошиблись?"

Вывод необходимо осуществить в модальное окно.) Написать валидатор.

3.задание #027 добавь в для примера текст "Пример". размер шрифта будет 24px. Добавь проверку в валитатор

Спецсимволы (Unicode) для текста
Если нужно вставить символ вне списка, используйте коды:
Ⅰ (&#8544;) — Один
Ⅱ (&#8545;) — Два
Ⅲ (&#8546;) — Три
Ⅳ (&#8547;) — Четыре
Ⅴ (&#8548;) — Пять


4.
Сделай рефакторинг структуры проекта с изоляцией учебных курсов.

**Формат новой структуры:**
- Корень проекта: только общие конфиги, глобальные тесты (если есть), общий readme.md, папки курсов.
- Каждый курс лежит в `/<course-name>` (название латиницей, пробелы заменить на дефисы).
- Внутри `/course-name`:
  - `/tasks` — задания
  - `/validators` — валидаторы
  - `/__tests__` — тесты курса
  - `readme.md` — описание курса

**Что нужно сделать:**
1. Проанализируй текущую структуру и предложи миграционный план.
2. Напиши shell-команды (или python/js скрипт) для переноса файлов.
3. Обнови все пути импортов в коде (например, `from ../validators` → `from ./validators` или через алиасы).
4. Убедись, что тесты запускаются после рефакторинга.

**Ограничение:** Не меняй логику работы кода, только структуру и пути.

Проанализируй проект. Напишите функционал который перехватывает вывод console.log() пользователя при нажатии на кнопку запустить и проверить и отображает результат в окне программы "Ваш результат"  

в topic Переменные и типы курса JS перед заданиями добавь иконку с сылкой на теорию, которая появляется в вкладке HTML. В ней теория топика для решения задач этого топика.


## Контекст проекта
educational-platform

## Технологии
HTML, CSS, JS

## Задание

----------------------------------------
для курса js-basics напиши топик "Циклы" по аналогии с "Функции" Придумай 10 простых заданий с возрастающей сложностью. Напиши валитаторы. Несколько тестов.

----------------------------------
Ты — эксперт по верстке. Создай тест «Основы HTML & CSS. Тест 3» из 15 заданий.
Структура — как в «Тесте 2» 
Уровень сложности — чуть выше предыдущего, средне.

Темы для включения:

HTML: семантические теги <header>, <footer>, <main>, <section>, <nav> 5 заданий

CSS: каскад и специфичность (вес селекторов: класс, id, тег), наследование свойств, группировка селекторов, псевдоклассы (:hover, :first-child, :last-child), псевдоэлементы (::before, ::after), единицы измерения (px, %,  — базовое отличие), базовый Flexbox (display: flex, flex-direction). 10 заданий
напиши валидаторы. добавь теорию по аналогии с Тесте 2
-------------------------------

напиши курс по python по аналогии с курсом js. 

Теория с подсветкой синтаксиса Python (можно использовать highlight.js или свою простую подсветку)

Интерактивные примеры кода с возможностью редактирования и запуска прямо в браузере (выполняя код через eval() на JS — но будьте осторожны)

Песочница для Python на фронтенде
Возможно используйте Skulpt (библиотеку на JS, которая интерпретирует Python)
Проверка упражнений
Сохранение прогресса
-------------------

курсы Основы HTML & CSS Тест I
Основы HTML & CSS Тест 2
Основы HTML & CSS Тест 3
JS должы быть вкладки для ввода html css js
для курса python только вкладка 
----------------
в курсе python решение заданий сразу выводиться в окно написания кода. Спрячь его.
во вкладку эталон нужно выводить пример результата работы готовой программы.
---------------------

 собери  единый красивый, структурированный курс по react с теорией. К каждому теоретическому блоку добавь 5 заданий по практике от простого к сложному. курс оформи ввиде html css js проекта. Должна быть возможность посмотреть правильную реалицию по клику.
--------------------

нужно сделать современный функционал, крутой внешний вид, добавить теория в каждую тему как для новичка, улучшить ux. 
-----------------
создай  красивый, структурированный курс по react с теорией. К каждому теоретическому блоку добавь по 10 заданий по практике от простого к сложному. курс оформи ввиде html css js проекта. Должна быть возможность посмотреть правильную реалицию по клику. Должно быть дана подробная теория темы. В том числе установка через vite. Система прогресса. Автопроверка заданий.

-------------------
Твоя задача — разработать полностью рабочий, автономный HTML/CSS/JS проект, который представляет собой интерактивный курс по React.
## Общие требования

1. **Стиль и оформление**:
   - Используй современный, минималистичный дизайн (тёмную тему )
   - Скролл внутри блоков, чистая типографика.

2. **Структура курса**:
Тема 1. Основы React: компоненты, JSX, рендеринг
Что такое React и зачем нужен

Настройка проекта через Vite

Структура React-приложения

Функциональные компоненты

JSX: правила, выражения, атрибуты

Встроенные стили и CSS-классы

ReactDOM.createRoot и рендеринг

Тема 2. Работа с пропсами (Props) и композиция
Передача данных в компоненты через props

Деструктуризация props

PropTypes и дефолтные значения

Children prop

Композиция компонентов (вложенность)

Передача функций через props (callback props)

Тема 3. Состояние компонента (useState) и события
Хук useState: объявление и обновление состояния

Обработка событий (onClick, onChange, onSubmit)

Формы и управляемые компоненты

Обновление состояния на основе предыдущего значения

Работа с объектами и массивами в состоянии

Тема 4. Эффекты (useEffect) и работа с API
Жизненный цикл функционального компонента

Хук useEffect: когда и зачем нужен

Пустой массив зависимостей (монтирование)

Зависимости (обновление при изменении)

Очистка эффектов (cleanup function)

Fetch / axios — получение данных из API

Обработка состояний загрузки и ошибок

Тема 5. Условный рендеринг, списки и ключи
if / тернарный оператор / && в JSX

Рендеринг массивов данных (map)

Атрибут key — зачем и как правильно выбирать

Фильтрация и сортировка списков

Динамические стили

Рендеринг компонентов в цикле

Тема 6. Хуки более глубоко (useRef, useMemo, useCallback)
useRef: доступ к DOM-элементам и хранение мутируемых значений

useMemo — кеширование вычислений

useCallback — стабильные функции

Когда нужны эти хуки (оптимизация)

Разница между useMemo и useCallback

Тема 7. Маршрутизация (React Router v6)
Установка и настройка React Router

BrowserRouter, Routes, Route

Ссылки (Link, NavLink)

Динамические маршруты (useParams)

Программная навигация (useNavigate)

Вложенные маршруты (Outlet)

Защищённые маршруты (PrivateRoute / защита страниц)

Тема 8. Управление состоянием (Context API и Redux Toolkit)
Проблема prop drilling

Context API: createContext, useContext, Provider

Разделение контекстов

Когда контекста недостаточно (введение в Redux)

Redux Toolkit: store, slice, reducer, actions

useDispatch и useSelector

Тема 9. Формы в React (React Hook Form + валидация)
Библиотека React Hook Form

Регистрация полей

Валидация (required, minLength, pattern)

Кастомная валидация

Обработка ошибок и их отображение

Асинхронные действия (createAsyncThunk)
   - Каждая тема содержит:
     - Заголовок
     - **Подробную теорию** (с примерами кода, пояснениями). Теория должна закрывать тему на уровне junior React разработчика.
     - Блок **"Установка через Vite"** — описать процесс создания проекта (`npm create vite@latest`, выбор React, запуск dev сервера) добавить в самую первую тему.

3. **Практические задания**:
   - К каждой теме приложить **10 заданий** по нарастанию сложности (от лёгкого к сложному).
   - Каждое задание — это текстовое описание того, что нужно сделать в коде.

4. **Интерактив и проверка**:
   - Рядом с каждым заданием должна быть кнопка **"Показать правильную реализацию / решение"**.
     - По клику открывается (или появляется под заданием) **красивый код** на JSX/JS с правильным решением.
   - **Автопроверка заданий**:
     - Для каждого задания добавь чекбокс или переключатель «Выполнено».
     - Пользователь может отмечать задание выполненным вручную, но система должна запоминать состояние (localStorage) и показывать общий прогресс.
     - При нажатии на "Проверить" к заданию должно анализироваться, что пользователь написал.  если пользователь считает, что сделал, и отмечает чекбокс, система может подсветить "Молодец, сверь с решением". 

   - **Система прогресса**:
     - Верхняя панель с общим процентом выполненных заданий (из всех тем).
     - Прогресс сохраняется в localStorage, чтобы не сбрасывался после перезагрузки страницы.

5. **Техническая реализация**:
   - Вся страница — один файл `index.html`.
   - Используй `<style>` для CSS и `<script>` для JavaScript.
   - Данные о темах, заданиях, эталонных решениях храни в JavaScript-объекте.
   - Кнопки "Показать решение" динамически создают overlay или блок с кодом (используй подсветку синтаксиса через `pre` и `code`).
   - При клике на "Показать решение" в любом месте пользователю должно быть понятно, как должно выглядеть рабочее решение (JSX или чистый React-код, но так как это курс, можно показывать синтаксис компонента).
   - Учти, что React-код в демо-решениях не обязан исполняться прямо на странице, но должен быть корректным примером. (Если хочешь, можно добавить React через CDN и бабел, но упрощённо — достаточно показа кода).

6. **Дополнительные критерии**:
   - Чистый, хорошо откомментированный код.
   - Не должно быть внешних зависимостей, кроме шрифтов (Google Fonts можно).
   - Рабочие интерактивные элементы (переключение тем, прогресс, кнопки решений, автопроверка).
   - Минимум багов, корректная работа в современных браузерах.

**Твоя главная задача — выдать полностью законченный HTML-файл, который я могу сохранить как `.html` и открыть в браузере, и сразу видеть удобный, красивый курс с теорией, заданиями, кнопками решений и прогрессом.**